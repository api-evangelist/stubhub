{
  "opencollection": "1.0.0",
  "info": {
    "name": "StubHub Account Addressess ListingConstraints API",
    "version": "v2"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "client_credentials",
      "accessTokenUrl": "https://account.stubhub.com/oauth2/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "ListingConstraints",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "List listing constraints for an event",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://api.stubhub.net/events/:eventId/listingconstraints",
            "params": [
              {
                "name": "eventId",
                "value": "",
                "type": "path"
              }
            ],
            "auth": {
              "type": "oauth2",
              "flow": "client_credentials",
              "accessTokenUrl": "https://account.stubhub.com/oauth2/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "List listing constraints for an event"
        },
        {
          "info": {
            "name": "Get listing constraints for a seller listing",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://api.stubhub.net/sellerlistings/:listingId/constraints",
            "params": [
              {
                "name": "listingId",
                "value": "",
                "type": "path"
              }
            ],
            "auth": {
              "type": "oauth2",
              "flow": "client_credentials",
              "accessTokenUrl": "https://account.stubhub.com/oauth2/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Get listing constraints for a seller listing"
        },
        {
          "info": {
            "name": "Get listing constraints for a requested event",
            "type": "http"
          },
          "http": {
            "method": "PUT",
            "url": "https://api.stubhub.net/listingconstraints",
            "auth": {
              "type": "oauth2",
              "flow": "client_credentials",
              "accessTokenUrl": "https://account.stubhub.com/oauth2/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Get listing constraints for a requested event"
        }
      ]
    }
  ],
  "bundled": true
}